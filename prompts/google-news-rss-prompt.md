# Google News RSS Prompt

```
Build or update RSS items for Google News:
Input: an array of post frontmatter objects.
Output: an RSS channel with <item> entries that include: title, link, guid, pubDate (RFC-822), description, author, category.
Return valid XML only.
```
